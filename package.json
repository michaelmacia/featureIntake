{
  "name": "feature-intake",
  "version": "0.1.0",
  "private": true,
  "description": "Intake form and triage board for business feature requests",
  "main": "server/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed.js",
    "gen:data": "node test-data/generate.js",
    "test": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  }
}
